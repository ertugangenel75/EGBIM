<Window x:Class="EGBIMOTO.Addin.UI.ManifestInputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Manifest Girdileri" SizeToContent="Height"
        Width="520" MinHeight="160" MaxHeight="680"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        Background="#1E1E2E">
    <Window.Resources>
        <SolidColorBrush x:Key="BgPanel"      Color="#2A2A3E"/>
        <SolidColorBrush x:Key="BgRow"        Color="#252538"/>
        <SolidColorBrush x:Key="AccentPurple" Color="#BD93F9"/>
        <SolidColorBrush x:Key="AccentCyan"   Color="#8BE9FD"/>
        <SolidColorBrush x:Key="AccentGreen"  Color="#50FA7B"/>
        <SolidColorBrush x:Key="AccentWarn"   Color="#FFB86C"/>
        <SolidColorBrush x:Key="AccentRed"    Color="#FF5555"/>
        <SolidColorBrush x:Key="TextMain"     Color="#F8F8F2"/>
        <SolidColorBrush x:Key="TextMuted"    Color="#9090A8"/>
        <SolidColorBrush x:Key="BorderColor"  Color="#44445A"/>
        <Style x:Key="MainBtn" TargetType="Button">
            <Setter Property="Padding" Value="18,8"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsEnabled" Value="False"><Setter Property="Opacity" Value="0.35"/></Trigger>
                            <Trigger Property="IsMouseOver" Value="True"><Setter Property="Opacity" Value="0.82"/></Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Margin="0,0,0,10">
            <TextBlock Text="⚡  Manifest Girdileri" FontSize="15" FontWeight="Bold" Foreground="{StaticResource AccentPurple}"/>
            <TextBlock x:Name="txtManifestName" Foreground="{StaticResource TextMuted}" FontSize="11" Margin="0,2,0,0"/>
        </StackPanel>
        <Border Grid.Row="1" Background="{StaticResource BgPanel}" CornerRadius="6" Padding="10,7"
                BorderBrush="{StaticResource BorderColor}" BorderThickness="1" Margin="0,0,0,12">
            <TextBlock x:Name="txtDescription" Foreground="{StaticResource TextMuted}" FontSize="10" TextWrapping="Wrap"/>
        </Border>
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" MaxHeight="400" Margin="0,0,0,14">
            <StackPanel x:Name="pnlInputs"/>
        </ScrollViewer>
        <!-- v11: Async yükleme göstergesi -->
        <TextBlock Grid.Row="3" x:Name="txtLoadingIndicator"
                   Foreground="#FFB86C" FontSize="10" Margin="0,0,0,8"
                   Visibility="Collapsed"/>
        <Grid Grid.Row="4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="8"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" x:Name="txtValidation" Foreground="{StaticResource AccentWarn}" FontSize="10" VerticalAlignment="Center"/>
            <Button Grid.Column="1" x:Name="btnOk" Content="✓  Çalıştır" Style="{StaticResource MainBtn}"
                    Background="{StaticResource AccentPurple}" Foreground="#1E1E2E" Click="BtnOk_Click"/>
            <Button Grid.Column="3" Content="İptal" Style="{StaticResource MainBtn}"
                    Background="#44445A" Foreground="{StaticResource TextMuted}" Click="BtnCancel_Click"/>
        </Grid>
    </Grid>
</Window>
